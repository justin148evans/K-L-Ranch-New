K & L Ranch — FIXED CLEAN URL BUILD

This version is self-contained:
- CSS is embedded in each page.
- JavaScript is embedded in each page.
- Ranch photos are embedded in each page.
- No separate assets folder is required.

Upload ALL of these items to the public/root folder of your website:
  index.html
  our-ranch/
  eggs-chickens/
  gallery/
  contact/

Clean URLs:
  /
  /our-ranch/
  /eggs-chickens/
  /gallery/
  /contact/

IMPORTANT:
Delete the older website files from the server first so old CSS/HTML files
do not conflict with this build, then upload this version.
